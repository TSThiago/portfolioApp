import { Text, View } from "react-native"
import { ContactContainer } from "./contact.style"

const Contact = () => {
    return (
        <ContactContainer>
        </ContactContainer>
    );
};

export default Contact;